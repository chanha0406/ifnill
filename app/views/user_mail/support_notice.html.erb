<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>안녕하세요, <%=@user.name%>님, ifnill입니다.</p>
    <p>즐거운 소식을 알려드립니다.</p>
    <p><%=@support_user.name%>님 께서 
      <% @itemlist.each do |i| %>
      <% if i == @itemlist.last  %>
        <%= i %>
      <% else %>
        <%= i %>, 
      <% end %>
    <% end %>
    후원을 신청하셨습니다.</p>
    <p><%=@support_user.name%>님의 이메일 주소는 <a href="mailto:<%=@support_user.email%>"><%=@support_user.email%></a>입니다.</p>
    <p>fuller <%=@support_user.name%>님과 연락을 통해 버킷리스트를 실현하시길 바랍니다.</p>
    <p>여러분의 꿈을 응원합니다.</p>
    <h4><a href="http://ifnill.com">ifnill</h4>
   </body>
</html>